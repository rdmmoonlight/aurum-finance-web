<nav id="sidebar" class="sidebar text-white d-flex flex-column align-items-center py-3">
    <!-- Brand Header (Static Logo Icon) -->
    <div class="mb-4 brand-container text-center">
        <a asp-controller="Home" asp-action="Dashboard" class="text-warning" title="Aurum Finance v10.0.0">
            <i class="bi bi-shield-lock-fill fs-3"></i>
        </a>
    </div>

    <!-- Menu Items (Static Icons - Click to open page directly) -->
    <ul class="nav flex-column align-items-center w-100 flex-grow-1">
        <li class="nav-item mb-2">
            <a asp-controller="Home" asp-action="Dashboard" class="nav-link text-white icon-btn" title="Dashboard">
                <i class="bi bi-house-door fs-4"></i>
            </a>
        </li>
        <li class="nav-item mb-2">
            <a asp-controller="Analytics" asp-action="Index" class="nav-link text-white icon-btn" title="Analytics">
                <i class="bi bi-graph-up fs-4"></i>
            </a>
        </li>
        <li class="nav-item mb-2">
            <a asp-controller="Transaction" asp-action="Index" class="nav-link text-white icon-btn" title="Transactions">
                <i class="bi bi-wallet2 fs-4"></i>
            </a>
        </li>
        <li class="nav-item mb-2">
            <a asp-controller="GeneralJournal" asp-action="Index" class="nav-link text-white icon-btn" title="General Journal">
                <i class="bi bi-journal-bookmark fs-4"></i>
            </a>
        </li>
    </ul>

    <!-- Bottom User Section (Single User Icon at the Bottom) -->
    <div class="mt-auto dropup">
        <a href="#" class="nav-link text-white icon-btn" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false" title="@(User.Identity?.Name ?? "User")">
            <i class="bi bi-person-circle fs-3"></i>
        </a>

        <!-- Popover Dropdown (Settings & Sign Out) -->
        <ul class="dropdown-menu dropdown-menu-dark shadow ms-2 mb-2" aria-labelledby="userDropdown">
            <li class="dropdown-header">@(User.Identity?.Name ?? "User")</li>
            <li><hr class="dropdown-divider"></li>
            <li>
                <a class="dropdown-item d-flex align-items-center" asp-controller="Settings" asp-action="Index">
                    <i class="bi bi-gear me-2"></i> Settings
                </a>
            </li>
            <li>
                <a class="dropdown-item d-flex align-items-center text-danger" asp-controller="Auth" asp-action="Logout">
                    <i class="bi bi-box-arrow-right me-2"></i> Sign Out
                </a>
            </li>
        </ul>
    </div>
</nav>